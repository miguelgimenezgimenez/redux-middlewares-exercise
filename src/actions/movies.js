
export const getDiscover = () => {
    //1: we need to call the api client and get the discover movies 
   // 2: we need to set the movies in the redux store.
   // 3: we need to set the lists in the redux store.
   return {}
   };
export const getCategories = () => {
    // 1: we need to get all the categories.
    // 2: for each category we need to iterate over call the api and get the category movies.
    // 3: we need to set movies and lists for each category.
};



